<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%=product.name%> </title>
</head>
<body>
    <h1>Edit <%=product.name%> </h1>
    <form action="/products/<%=product._id%>?_method=PUT" method="POST">
        <label for="name">Product Name:</label>
        <input type="text" name="name" id="name" placeholder="product name" value="<%=product.name%>">

        <label for="price">Product Price:</label>
        <input type="number" name="price" step=".01" id="price" placeholder="product price" value="<%=product.price%>">

        <label for="category">Product Category:</label>
        <select name="category" id="category">
            <!-- dynamically add categories -->
            <!-- selects product category -->
            <% for (let cat of categories) {%> 
            <option value="<%=cat%>"
                <%=cat === product.category ? 'selected' : '' %>>
                <%=cat%>
            </option>
            <% } %> 
        </select>
        <button>Submit</button>
    </form>
    <a href="/products/<%=product._id%>">Cancel</a>
</body>
</html>